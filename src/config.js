import dotenv from 'dotenv';
dotenv.config();

export const {
  COMETCHAT_API_KEY,
  COMETCHAT_APP_ID,
  BOT_UID,
  COMETCHAT_RECEIVER_UID,
  HANGOUT_ID,
  CHAT_AVATAR_ID,
  CHAT_NAME,
  CHAT_COLOUR
} = process.env;
